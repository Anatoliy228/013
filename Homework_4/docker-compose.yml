version: '3.7'

services:
    
    centos_container:
        build:
            dockerfile: Dockerfile
        ports:
            - "8081:8081"
        
            
            
    mysql:
        build:
            dockerfile: Dockerfile-mysql
        ports:
            - "3306:3306"
        restart: always
