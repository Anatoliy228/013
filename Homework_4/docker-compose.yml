version: '3.7'

services:
    
    centos_container:
        build:
            dockerfile: Dockerfile-ubuntu_apache
        ports:
            - "8081:8081"
        restart: always
            
            
    mysql:
        build:
            dockerfile: Dockerfile-mysql
        ports:
            - "3306:3306"
        restart: always
